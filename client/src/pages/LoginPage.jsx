// LoginPage - Halaman untuk login dan register
// Komponen ini akan menampilkan modal login/register
// File ini sebagai placeholder, logika akan dipindahkan dari App.jsx

const LoginPage = () => {
  return (
    <div>
      {/* Login/Register UI akan dipindahkan dari App.jsx */}
      <p>Login Page</p>
    </div>
  );
};

export default LoginPage;
